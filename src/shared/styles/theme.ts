import { Colors, Size, Font, ModerateSize, Dimension } from "@/shared/styles";

export const Theme = {
  colors: Colors,
  sizes: Size,
  text: Font,
  moderateSize: ModerateSize,
  dimension: Dimension,
};
