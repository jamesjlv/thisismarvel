import {
  ICharacterComicsResultsModel,
  ICharactersResultsModel,
  IComicsResultsModel,
  IEventsResultsModel,
  ISeriesResultsModel,
} from "@/domain";

export type SelectedFilter = "characters" | "comics" | "series" | "events";
