export enum Routes {
  SignIn = "SignIn",
  Welcome = "Welcome",
  Login = "Login",
  Home = "Home",
  ForgotPassword = "ForgotPassword",
}
export enum Stacks {
  Authorized = "Authorized",
  Authorization = "Authorization",
}
